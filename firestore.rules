rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules for assignment/demo app.
    // Tighten this before production release.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
